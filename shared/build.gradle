// POM file modifications
uploadArchives {
  repositories {
    mavenDeployer {
      pom.project {
        name 'Enroscar Shared'
        description 'IO and other utilities used by other Enroscar components.'
      }
    }
  }
}

dependencies(
    project.dependOnProjects([':enroscar-beans'])
)
