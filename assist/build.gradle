archivesBaseName = 'enroscar-assist'

dependencies {
  compile project(path : ':sdk-dep', configuration : 'jarPackage')
  compile project(':beans')
  compile project(':shared')
  compile project(':net')
  compile project(':stats')
}
