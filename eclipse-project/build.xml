<project name="enroscar" basedir="." default="help">
	<condition property="cintegration.home" value="${basedir}/../cintegration" else="${basedir}/../../cintegration">
		<available file="${basedir}/../cintegration" type="dir" />
	</condition>
	<import file="${cintegration.home}/main-build.ant"/>
</project>
