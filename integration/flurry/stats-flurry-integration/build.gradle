repositories {
  flatDir {
    dirs 'libs'
  }
}

dependOnProjects([':stats'])

dependencies {
  
  provided 'com.flurry:flurry:3.2.1'
    
}

// POM file modifications
uploadArchives {
  repositories {
    mavenDeployer {
      pom.project {
        name 'Enroscar Stats with Flurry integration'
        description 'Statistics manager abstraction for integration with analytics services.'
      }
    }
  }
}
